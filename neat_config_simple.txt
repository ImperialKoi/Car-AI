# NEAT Configuration for Racing Car AI - Educational Version
# Each section is explained for learning purposes

[NEAT]
# How NEAT measures success
fitness_criterion     = max
fitness_threshold     = 2000
pop_size              = 20
reset_on_extinction   = False

[DefaultGenome]
# Neural network activation functions
activation_default      = tanh
activation_mutate_rate  = 0.1
activation_options      = tanh sigmoid

# How nodes combine inputs
aggregation_default     = sum
aggregation_mutate_rate = 0.1
aggregation_options     = sum

# Node bias settings (maximum diversity for varied behavior)
bias_init_mean          = 0.0
bias_init_stdev         = 3.0
bias_max_value          = 30.0
bias_min_value          = -30.0
bias_mutate_power       = 1.5
bias_mutate_rate        = 0.8
bias_replace_rate       = 0.3

# How similar genomes are grouped
compatibility_disjoint_coefficient = 1.0
compatibility_weight_coefficient   = 0.5

# Connection mutation rates (simplified)
conn_add_prob           = 0.2
conn_delete_prob        = 0.2

# Connection settings
enabled_default         = True
enabled_mutate_rate     = 0.01

# Network structure
feed_forward            = True
initial_connection      = full_nodirect

# Node mutation rates
node_add_prob           = 0.1
node_delete_prob        = 0.1

# Network input/output structure
num_hidden              = 0
num_inputs              = 11
num_outputs             = 3

# Response settings (simplified)
response_init_mean      = 1.0
response_init_stdev     = 0.0
response_max_value      = 30.0
response_min_value      = -30.0
response_mutate_power   = 0.0
response_mutate_rate    = 0.0
response_replace_rate   = 0.0

# Weight mutation settings (maximum diversity for varied behavior)
weight_init_mean        = 0.0
weight_init_stdev       = 3.0
weight_max_value        = 30
weight_min_value        = -30
weight_mutate_power     = 1.5
weight_mutate_rate      = 0.9
weight_replace_rate     = 0.3

[DefaultSpeciesSet]
compatibility_threshold = 3.0

[DefaultStagnation]
species_fitness_func = max
max_stagnation       = 15
species_elitism      = 2

[DefaultReproduction]
elitism            = 2
survival_threshold = 0.2
